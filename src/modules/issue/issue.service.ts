import type { Request } from "express";

const createIssueIntoDB = async (payload: Request) => {
//   console.log("this is header : : : ", reqHeader);
};

export const issueService = {
  createIssueIntoDB,
};
